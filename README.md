#### Simple task to calculate number occurrence

in this branch it is done by using mutex

checkout to branch channel to see the implementation with 
channels and context
```bash
git checkout channel
```

##### Usage:
```bash
go run -race main.go
```
